set -g default-terminal "tmux-256color"
# Tell tmux the outer terminal supports true color (24-bit RGB), so it passes
# those escape sequences through instead of stripping them. The *256col* glob
# matches common terminal names like xterm-256color and alacritty-256color.
set -ga terminal-overrides ",*256col*:Tc"
